apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  namespace: notes-microservices
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user-service
  template:
    metadata:
      labels:
        app: user-service
    spec:
      containers:
      - name: user-service
        image: study-assistant/user-service:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 8000
        env:
        - name: DATABASE_URL
          value: "postgresql://postgres:password@postgres:5432/notes_microservices"
---
apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: notes-microservices
spec:
  selector:
    app: user-service
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
