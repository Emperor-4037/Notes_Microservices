apiVersion: apps/v1
kind: Deployment
metadata:
  name: study-session-service
  namespace: notes-microservices
spec:
  replicas: 1
  selector:
    matchLabels:
      app: study-session-service
  template:
    metadata:
      labels:
        app: study-session-service
    spec:
      containers:
      - name: study-session-service
        image: study-assistant/study-session-service:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 8000
        env:
        - name: DATABASE_URL
          value: "postgresql://postgres:password@postgres:5432/notes_microservices"
---
apiVersion: v1
kind: Service
metadata:
  name: study-session-service
  namespace: notes-microservices
spec:
  selector:
    app: study-session-service
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
